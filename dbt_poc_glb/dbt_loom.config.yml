# dbt-loom configuration for cross-project references
# Global project consumes public models from Italy and Switzerland
manifests:
  - name: dbt_poc_ita
    type: file
    config:
      path: ../dbt_poc_ita/target/manifest.json
  - name: dbt_poc_che
    type: file
    config:
      path: ../dbt_poc_che/target/manifest.json
